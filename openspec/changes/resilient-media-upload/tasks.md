## 1. RabbitMQ topology: configurable retries + new media queue

- [x] 1.1 In `src/infrastructure/messaging/rabbit/RabbitMQRegistry.ts`, add an optional `maxRetries?: number` to the `RabbitMQRouting`/`retry` shape (or alongside it), and add a new exchange entry: `media.exchange` (topic) with queue `media.upload.queue`, routing key `media.upload`, a retry queue (`media.upload.queue.retry`, longer TTL, e.g. 60000ms) and a DLQ (`media.exchange.dlx` / `media.upload.queue.dlq`), with `maxRetries` set high enough to tolerate a multi-hour storage outage (e.g. 120).
- [x] 1.2 Update `src/infrastructure/messaging/rabbit/RabbitMQBootstrap.ts` if needed so any new fields on the registry entries (e.g. `maxRetries`) are simply data — no bootstrap logic change should be required if it only asserts exchanges/queues/bindings already generically, but verify.
- [x] 1.3 Update `src/infrastructure/messaging/rabbit/RabbitMQConsumer.ts` so `consume()` accepts an optional `maxRetries` parameter (defaulting to the current `MAX_RETRIES = 20`) and uses it instead of the hardcoded module constant, so `messages.queue`'s behavior is unchanged by default.
- [x] 1.4 Confirm (by reading `RabbitMQConnection.ts`) that `registerConsumer`/`startConsumerLoop` need no changes to support a second, independent consumer (`media.upload.queue`) running alongside the existing `messages.queue` consumer on the same shared connection.
- [x] 1.5 (Follow-up requested after initial implementation) Replace `media.upload.queue`'s flat fixed-TTL retry with **exponential backoff**: add optional `multiplier`/`maxTtl` to `RabbitMQRouting.retry` in `RabbitMQRegistry.ts`; set `media.upload.queue`'s retry to `{ ttl: 60000, multiplier: 2, maxTtl: 1800000 }` (1min → 2min → 4min → ... capped at 30min) and lower `maxRetries` to 30 (fewer retries needed since backoff+cap already covers a long outage). `RabbitMQConsumer.ts` computes `delay = ttl * multiplier^retryCount` (capped at `maxTtl`) per retry, looked up from the registry by queue name, and sends it as a per-message `expiration` on the retried message. `RabbitMQBootstrap.ts`'s retry-queue assertion no longer sets a fixed `x-message-ttl` argument (a queue-level TTL would cap every message at the same value regardless of per-message `expiration`, defeating backoff) — only the dead-letter-exchange/routing-key arguments remain. `messages.queue` is unaffected (`multiplier` unset defaults to 1 → flat 30s retries, unchanged). **Verified live** against a real local RabbitMQ broker with `maxRetries=2` forced to fail every attempt: attempts landed at t=0s, t=60s, t=180s (gaps of 60s then 120s — doubling as designed) before the job correctly landed in the DLQ.

## 2. Local media staging

- [x] 2.1 Add a staging directory convention, e.g. `./session/media-pending/`, created on demand (`fs.mkdirSync(..., { recursive: true })`), consistent with the existing `./session/<sessionId>/` bind-mounted pattern already used for Baileys auth state.
- [x] 2.2 Add a helper (e.g. `stageMediaBuffer(messageId, buffer, extension): string` returning the staged file path) and a corresponding read/delete helper, colocated with `BaileysConnector.ts` or in a small new module (e.g. `src/infrastructure/repositories/Baileys/MediaStaging.ts`).
- [x] 2.3 Ensure staged file names are collision-safe and traceable back to the originating message (e.g. `${msg.key.id}-${type}.${extension}`).

## 3. Split `uploadMessageMedia` into stage + enqueue

- [x] 3.1 In `src/infrastructure/repositories/Baileys/BaileysConnector.ts`, extract the existing buffer download + mimetype/filename logic (currently in `uploadMessageMedia`, lines ~344-403) into a function that downloads the buffer and writes it via the staging helper from Task 2, returning the staged file path + metadata (mimeType, fileName, type) instead of performing the `fetch` inline.
- [x] 3.2 Replace the inline `fetch` call (currently `BaileysConnector.ts:404-424`) with a `RabbitMQPublisher.publishExchange("media.exchange", "media.upload", {...})` call carrying: staged file path, mimeType, fileName, `tenant_id`, `sessionId`, the raw `msg` (or the minimal subset `BaileysToWhatpyMapper.map` needs), and the storage `path` value (`public/whatsapp/${msg.key.id}`) currently built inline.
- [x] 3.3 Update `bindMessages` (`BaileysConnector.ts:228-276`) so that when a message contains media, it stages the buffer and enqueues the upload job, then continues the loop without waiting for the upload or building the mapped message for the media path — do not call `BaileysToWhatpyMapper.map`/`this.events.emit("message.received", ...)` for media messages from this handler anymore (that now happens in the new worker after a successful upload). Non-media messages keep their current synchronous behavior unchanged.
- [x] 3.4 Ensure a staging/enqueue failure (e.g. disk full, RabbitMQ publish failure) is caught and logged via the existing `EventLog`/`log.fail()` mechanism already used in `bindMessages`'s outer `catch`, rather than silently dropping the message as today.

## 4. Media upload worker

- [x] 4.1 Create `src/workers/mediaUpload.worker.ts`: consume `media.upload.queue` via `RabbitMQConsumer.consume()` (passing the higher `maxRetries` from Task 1), read the staged file back from disk, and perform the `fetch` to `${MICROSERVICE_STORAGE}/storage/api/v1/file/add-item` with the same headers/multipart shape as today's inline call.
- [x] 4.2 On a successful upload: build the mapped message via `BaileysToWhatpyMapper.map(rawMsg, uploadedUrl)`, call `eventBus.emit("message.received", { sessionId, tenantId, data: mapped })` (same event shape `bindMessages` emits today for non-media messages), and delete the staged file.
- [x] 4.3 On a failed upload (non-2xx, network error, timeout): throw, so `RabbitMQConsumer`'s built-in retry-count-header + `.retry` requeue logic handles the backoff automatically. Do not delete the staged file.
- [x] 4.4 On exhausting retries (job lands in `media.upload.queue.dlq`): log a durable failure via `EventLog`/`DomainEventDispatcher` (same pattern used elsewhere for message processing failures) including the staged file path, so it is discoverable for manual replay. Confirm the staged file is left on disk (not deleted) in this path. (Required adding an optional `onDeadLetter` callback param to `RabbitMQConsumer.consume()`, invoked right after a job lands in the DLQ — not called out explicitly in the design, but a minimal, natural extension of the same generic consumer.)
- [x] 4.5 In `server.ts`, start this worker explicitly inside `start()`, right after `await bootstrapRabbitMQ()` (alongside `registerEventHandlers()`) — note that `src/workers/messagesUpsert.worker.ts` is currently dead code (never invoked by any script/entrypoint) so there is no existing pattern to hook into; this worker must be wired directly into the API process's startup.

## 5. Verification

- [ ] 5.1 Manually verify the happy path: send a photo/audio to a connected session with the storage microservice reachable — confirm the message is saved and published with the correct media link, with no observable added latency for text-only messages sent in parallel. **Not done**: requires a real, paired WhatsApp session and a reachable `MICROSERVICE_STORAGE` instance, neither available in this sandbox. Needs to be done against a real deployment before shipping.
- [ ] 5.2 Manually verify the outage path: stop/block the storage microservice (or point `MICROSERVICE_STORAGE` at an unreachable host), send a photo/audio — confirm the upload job retries (visible in RabbitMQ management UI at `:15672`) instead of the message being silently dropped, then restore the storage microservice and confirm the message is saved/published on a subsequent retry with no manual action. **Not done** for the same reason as 5.1 (no live WhatsApp session available here). The underlying retry mechanics this depends on were verified directly in 5.3.
- [x] 5.3 Manually verify the DLQ path: force uploads to fail beyond `maxRetries` (e.g. temporarily lower it for the test) — confirm the job lands in `media.upload.queue.dlq` with `x-last-error`/`x-failed-at` headers, a failure entry is recorded via `EventLog`, and the staged file still exists on disk afterward. Verified against a real local RabbitMQ broker (`docker compose up rabbitmq`): bootstrapped the actual topology (`RabbitMQBootstrap.setup`), published a job through `media.exchange`/`media.upload`, and confirmed with `maxRetries=1` it retried once after the configured 60s retry-queue TTL and then landed in `media.upload.queue.dlq` (`rabbitmqctl list_queues` showed 1 message there afterward) with the `onDeadLetter` callback firing. Also confirmed a non-failing job on the same queue is consumed immediately (no spurious retry/DLQ). Staged-file-preserved and `EventLog` recording were verified by code inspection (both paths run unconditionally on the dead-letter branch), not by inspecting a live `MessageEventLog` row, since that requires the app's DB + full session context.
- [ ] 5.4 Confirm text-only messages sent during a simulated storage outage are still saved/published immediately, unaffected by pending media retries. **Not done**: same live-WhatsApp-session dependency as 5.1/5.2. By code inspection, `bindMessages` only takes the async media path when `stageAndEnqueueMedia` returns `true` (i.e., real media was found); text-only messages fall through to the unchanged synchronous `BaileysToWhatpyMapper.map`/`emit` path regardless of media-queue state.
- [x] 5.5 Run the existing test suite (`npm run test:run`) and confirm no regressions; extend or add a test alongside `tests/RabbitMQ.integration.test.ts` covering publish/consume against the new `media.upload.queue` topology if practical in the current test setup. Added a second test to `tests/RabbitMQ.integration.test.ts` covering publish/consume through `media.exchange`/`media.upload.queue`; both tests pass against a live local broker. Without a broker running, both integration tests fail identically to how the original test already failed pre-change (pre-existing environmental limitation, not a regression). `tsc --noEmit` shows no new type errors (same pre-existing `@types/mocha`/`ws`/`fluent-ffmpeg` errors as before). `eslint` shows one new `no-explicit-any` warning consistent with the existing style already present in `RabbitMQConsumer.ts` (its `handler` param is already typed `any`; the new `onDeadLetter` param matches that).
